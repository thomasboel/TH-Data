When SubCategories are added they are added in the beginning and not the end, this needs fixing.

The current way of adding listing's makes too many spaces here and there, fix this along with the implementation of TableListing's.

Support for DataFile Handlers in a Util Class for that will be very handy for users using this System/API.

~Encryption Code

~Decryption/Deciphering Code

~User Util Classes